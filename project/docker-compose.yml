version: "3.9"

services:
  mysql:
    image: mysql:8.0
    container_name: hotel-ops-mysql
    environment:
      MYSQL_ROOT_PASSWORD: root123
      MYSQL_DATABASE: hotel_ops
    ports:
      - "3306:3306"
    volumes:
      - ./backend/sql:/docker-entrypoint-initdb.d

